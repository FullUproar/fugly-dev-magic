{
  "name": "gpt-relay-api",
  "version": "1.0.0",
  "description": "Relay API for Custom GPT to access local development server",
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  }
}